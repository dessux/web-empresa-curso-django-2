from django.apps import AppConfig


class ServicesConfig(AppConfig):
    name = 'services'

    # Modif. /services/apps.py con verbose_name = “Gestor de servicios”
    verbose_name = 'Gestor de servicios'
